import { MeasuresTranslatorPipe } from './measures-translator.pipe';

describe('MeasuresTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new MeasuresTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
