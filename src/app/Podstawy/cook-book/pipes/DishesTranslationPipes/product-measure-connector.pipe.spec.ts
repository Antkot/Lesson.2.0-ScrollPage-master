import { ProductMeasureConnectorPipe } from './product-measure-connector.pipe';

describe('ProductMeasureConnectorPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductMeasureConnectorPipe();
    expect(pipe).toBeTruthy();
  });
});
