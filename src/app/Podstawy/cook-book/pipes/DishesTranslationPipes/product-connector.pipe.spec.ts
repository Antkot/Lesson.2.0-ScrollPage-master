import { ProductConnectorPipe } from './product-connector.pipe';

describe('ProductconnectorPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductConnectorPipe();
    expect(pipe).toBeTruthy();
  });
});
