import { ProductTranslationPipe } from './product-translation.pipe';

describe('ProductTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
