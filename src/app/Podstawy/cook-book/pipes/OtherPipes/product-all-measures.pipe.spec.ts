import { ProductAllMeasuresPipe } from './product-all-measures.pipe';

describe('ProductAllMeasuresPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductAllMeasuresPipe();
    expect(pipe).toBeTruthy();
  });
});
