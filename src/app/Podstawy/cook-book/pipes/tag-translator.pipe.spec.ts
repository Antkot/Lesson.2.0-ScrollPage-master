import { TagTranslatorPipe } from './tag-translator.pipe';

describe('TagTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new TagTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
