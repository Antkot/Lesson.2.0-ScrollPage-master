import { DishTranslatorPipe } from './dish-translator.pipe';

describe('DishTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DishTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
