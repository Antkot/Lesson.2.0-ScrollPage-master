import { ProductMeasureDataPipe } from './product-measure-data.pipe';

describe('ProductMeasureDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductMeasureDataPipe();
    expect(pipe).toBeTruthy();
  });
});
